<!DOCTYPE html>

<html>
    <head>
        <title>Login to User Account</title>
        <link rel="stylesheet" href="Master_style.css" text="text/css">
    </head>
    <body>
        <div class="main">
            <!-- If redirected from the log out there will be this text here -->
            <% if(locals.logout) { %>
            <h2> <%= logout %> </h2>
            <% } %>
            <h2>User Login</h2>
            <form action="/UserLogin" method="post">
                <fieldset>
                    <label for="username">Enter Username</label>
                    <input type="text" name="username" id="username" placeholder="Enter Username">
                    <br>
                    <input type="password" name="password" id="password" placeholder="Enter Password">
                    <br>
                    <input type="submit" value="Login to Client Account">
                </fieldset>
            </form>
            
            <p>Current Logged in user:</p>
            <%= locals.user ? user : "Not logged in" %> 
            <!--Insert partial template for the footer-->
            <%- include ("partials/HandyNavFooter.ejs") %>
        </div>
    </body>
</html>